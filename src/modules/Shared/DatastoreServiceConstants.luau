local DatastoreServiceConstants = {
	-- General Parameters
	STUDIO_MODE = true, -- If in studio it won't push any data to the datastore.
	AUTO_SAVE_DELAY = 10,
	DATASTORE_VERSION = 1,

	-- Datastore Parameters
	DEFAULT_DATASTORE_NAME = "PlayerDataStore",
	GROUP_RANK_THRESHOLD = nil, -- Update Later
}

return DatastoreServiceConstants
